# Plan for Implementing Kilo CLI Launch

## Objective
Update the session bar button to launch Kilo CLI instead of Claude Code, and change the button text to "Kilo".

## Current State Analysis
- Button currently shows "⚡ Kilocode" with onclick="kilo()" but function doesn't exist
- Server endpoint `/api/sessions/:id/open-terminal` launches `claude` CLI
- Need to update everything to use `kilo` command instead

## Changes Required
1. ✅ Update button text from "⚡ Kilocode" to "Kilo" in `public/index.html`
2. ✅ Update server.js to use `kilo` command instead of `claude` in open-terminal endpoint
3. ✅ Rename JavaScript function from `openInClaude()` to `openKilo()` and update onclick handler

## Files Modified
- `public/index.html` - Button text and onclick handler (line 2775)
- `public/index.html` - Rename `openInClaude()` function to `openKilo()` (line 7155)
- `server.js` - Replace `claude` commands with `kilo` in open-terminal endpoint (lines 4450, 4456, 4463)

## Implementation Steps
1. ✅ Update HTML button text and onclick
2. ✅ Rename JavaScript function
3. ✅ Update server-side command execution
4. Test the functionality