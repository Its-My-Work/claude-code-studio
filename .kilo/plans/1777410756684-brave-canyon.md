# Plan for Updating Session Bar Button - COMPLETED

## Objective
Replace the "Claude Code" button text with "Kilocode" and change its onclick handler from `openInClaude()` to `kilo()` in the session bar.

## Context
- Location: `public/index.html` line 2775
- Current element: `<a id="sessBarOpenBtn" ... onclick="openInClaude()">⚡ Claude Code</a>`
- This is part of the session bar UI that appears during active chat sessions

## Changes Required
1. ✅ Update the text content from "⚡ Claude Code" to "⚡ Kilocode"
2. ✅ Change the onclick attribute from `openInClaude()` to `kilo()`

## Files Modified
- `public/index.html` - Single line edit applied successfully

## Validation
- ✅ Button now displays "Kilocode" instead of "Claude Code"
- ✅ onclick function call updated to `kilo()`
- ✅ Session bar HTML structure preserved