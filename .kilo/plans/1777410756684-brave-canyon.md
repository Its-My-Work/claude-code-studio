# Plan: Remove Remote Access (Tunnel) Feature

## Objective
Полностью удалить функцию Remote Access (туннель) из кода, ничего не сломав.

## Files to Modify
1. `public/index.html` - UI section, JS functions, CSS, i18n
2. `public/locales/en.json` - i18n strings
3. `public/locales/ru.json` - i18n strings
4. `server.js` - API endpoints, tunnel manager init, Telegram bot commands

## Changes Required

### 1. public/index.html

#### 1.1 Remove CSS (lines ~1720-1726)
- Remove `.tn-provider-card`, spinner styles

#### 1.2 Remove HTML section (lines ~2433-2488)
- Remove entire `<div class="sec">` with id `tunnelBody`
- Section title with `toggleSec('tunnel')`
- Provider cards for cloudflared/ngrok
- Ngrok auth input
- Start/Stop button
- Error display
- URL display box

#### 1.3 Remove i18n strings (lines ~4019, 4247)
- Remove: `sec.tunnel`, `tn.hint`, `tn.provider`, `tn.cf.desc`, `tn.ng.desc`, `tn.ng.get_token`, `tn.start`, `tn.url`, `tn.send_tg`

#### 1.4 Remove JS functions (lines ~11439-11836)
- Remove: `tunnelSelectProvider`, `tunnelToggleNgrokPwd`, `tunnelSetError`, `tunnelSetBtnState`, `tunnelLoadStatus`, `tunnelStart`, `tunnelToggle`, `tunnelStop`, `tunnelCopyUrl`, `tunnelSendToTelegram`, `tunnelUpdateTgButton`
- Remove: `_tunnelRunning`, `_tunnelStarting` variables
- Remove: `setTimeout(tunnelLoadStatus, 300)`
- Remove: ws handlers for `tunnel_url`, `tunnel_closed`

#### 1.5 Remove section toggles (lines ~11885, 11922)
- Remove `tunnel` from `sections` object
- Remove `tunnel: 'tunnelBody'` from `secs` mapping

### 2. public/locales/en.json & ru.json

#### Remove keys:
- `sec.tunnel`

### 3. server.js

#### 3.1 Remove require
- Remove: `const TunnelManager = require('./tunnel-manager')`

#### 3.2 Remove tunnel manager init (lines ~5076-5097)
- Remove: `initTunnelManager()` function
- Remove: `tunnelManager` variable
- Remove: event listeners `url`, `close`

#### 3.3 Remove Telegram bot commands (lines ~5381-5421)
- Remove: `tunnel_get_status`
- Remove: `tunnel_start`
- Remove: `tunnel_stop`
- Remove: `tunnel_status`

#### 3.4 Remove API endpoints (lines ~5557-5636)
- Remove: `GET /api/tunnel/status`
- Remove: `POST /api/tunnel/start`
- Remove: `POST /api/tunnel/notify-telegram`
- Remove: `POST /api/tunnel/stop`

#### 3.5 Remove init and cleanup (lines ~7156-7157, 7174, 7188-7189)
- Remove: `initTunnelManager()` call on server start
- Remove: tunnel status from `/api/health`
- Remove: tunnel stop in graceful shutdown

## Validation
After removal:
1. Verify server starts without errors: `node server.js`
2. Verify UI loads correctly
3. Verify other sections (MCP, Projects, SSH Hosts, Telegram) still work
4. Verify chat functionality works

## Risk Assessment
- **LOW** - Tunnel is isolated feature, removal shouldn't affect other functionality
- Verify no references to tunnel in other sections before deployment