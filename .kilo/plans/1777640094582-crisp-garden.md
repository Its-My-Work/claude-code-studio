# Полное удаление Claude из проекта и переход только на Kilo

## Цель
Полностью удалить все ссылки на "claude" из кодовой базы и настроить проект для работы исключительно с Kilo. Убрать зависимость от переменной AGENT_ENGINE и всегда использовать Kilo.

## Что нужно удалить/заменить

### 1. Файлы для удаления
- `claude-cli.js` - заменить на `kilo-cli.js`
- `backends/claude-backend.js` - заменить на `backends/kilo-backend.js`
- `claude-ssh.js` - заменить на `kilo-ssh.js`

### 2. Код для удаления
- Все условные блоки `if (engine === 'claude')`
- Переменная окружения `AGENT_ENGINE` (всегда использовать kilo)
- Пути к бинарным файлам claude
- Константы: `GLOBAL_CLAUDE_DIR`, `ClaudeCLI`, `ClaudeSSH`
- Поле БД `claude_session_id` → `kilo_session_id`

### 3. Строки и конфигурации для замены
- Все упоминания "claude" в комментариях и логах
- Модель `anthropic/claude-3-5-sonnet-20241022` из списка моделей
- Директория `.claude` → `.kilo` в домашней папке

### 4. Существующая инфраструктура Kilo
- Конфигурация `.kilo/kilo.jsonc` уже существует
- Пути для бинарного файла kilo уже добавлены
- Некоторые UI элементы уже переключены на "kilocode"

## План полного удаления Claude

### Шаг 1: Переименование и удаление файлов
- Переименовать `claude-cli.js` → `kilo-cli.js`
- Переименовать `backends/claude-backend.js` → `backends/kilo-backend.js`
- Переименовать `claude-ssh.js` → `kilo-ssh.js`
- Удалить все условные блоки с `engine === 'claude'`
- Заменить все импорты на новые имена файлов

### Шаг 2: Удаление переменной AGENT_ENGINE
- Убрать все проверки `process.env.AGENT_ENGINE`
- Всегда использовать код для kilo
- Удалить логику выбора между claude и kilo

### Шаг 3: Обновление констант и путей
- `GLOBAL_CLAUDE_DIR` → `GLOBAL_KILO_DIR`
- `ClaudeCLI` → `KiloCLI`
- `ClaudeSSH` → `KiloSSH`
- Обновить все пути к бинарным файлам на kilo
- Изменить глобальную директорию с `.claude` на `.kilo`

### Шаг 4: Обновление базы данных и моделей
- Переименовать поле `claude_session_id` → `kilo_session_id`
- Удалить модель `anthropic/claude-3-5-sonnet-20241022` из списка
- Обновить все связанные функции работы с сессиями

### Шаг 5: Очистка строк и комментариев
- Заменить все "claude" на "kilo" в комментариях и логах
- Обновить все строковые литералы
- Проверить конфигурационные файлы

### Шаг 6: Тестирование
- Проверить работу без AGENT_ENGINE
- Убедиться, что все функции работают только с kilo
- Протестировать создание и восстановление сессий

## Риски
- Изменение схемы БД сломает существующие сессии (нужна миграция данных)
- Переименование глобальной директории `.claude` → `.kilo` может затронуть другие проекты, использующие claude
- Нужно убедиться, что бинарный файл kilo установлен и доступен
- Потеря обратной совместимости с claude

## Преимущества
- Упрощение кода (удаление условных блоков)
- Четкая архитектура только для Kilo
- Уменьшение размера кодовой базы
- Лучшая поддержка и развитие только для Kilo

## Следующие шаги
Если план одобрен, приступить к поэтапному удалению claude и переходу исключительно на kilo.</content>
<parameter name="filePath">/home/user/Projects/claude-code-studio/.kilo/plans/1777640094582-crisp-garden.md